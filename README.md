# InfTec_Lab5

**Лабораторна робота №5**  
Курс: Інформаційні технології  
Автор: *Andrashko*  
Тип проєкту: Веб-застосунок із PHP, HTML та SQL базою даних

---

## 📌 Опис проєкту

Цей репозиторій містить реалізацію лабораторної роботи №5 з курсу **Інформаційні технології**.  
Проєкт є простим веб-застосунком (можливо, тестовим магазином), що включає:

- Файли PHP для серверної логіки;
- HTML та CSS для інтерфейсу;
- SQL-скрипт для створення тестової бази даних.

---

## 🗂️ Структура репозиторію

├── app/ — серверна логіка PHP
├── css/ — стилі для сторінок
├── index.html — основна сторінка інтерфейсу
├── index.php — точка входу PHP-застосунку
├── test_shop.sql — SQL-скрипт для створення тестової БД
├── .gitignore
├── .htaccess

---

## 🚀 Як запустити

Щоб запустити проєкт локально:

1. **Встановіть локальний сервер (наприклад, XAMPP або MAMP).**
2. Скопіюйте проєкт у папку `htdocs` або `www`.
3. Імпортуйте SQL-файл `test_shop.sql` у ваш сервіс бази даних (наприклад, MySQL).
4. Відкрийте браузер і перейдіть до:

